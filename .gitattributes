# Keep repository text files consistent across Windows and Unix checkouts.
* text=auto eol=lf

# Binary assets and bundled dependencies must never be line-ending normalized.
*.jar binary
*.png binary
